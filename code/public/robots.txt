# Robots.txt for The Kove Design
# https://kovedesign.in

# Allow all search engines to crawl everything
User-agent: *
Allow: /

# Disallow crawling of API routes and admin paths (if any in the future)
Disallow: /api/
Disallow: /_next/
Disallow: /admin/

# Sitemap location
Sitemap: https://kovedesign.in/sitemap.xml

# Crawl-delay (optional, helps prevent server overload)
# Crawl-delay: 1
